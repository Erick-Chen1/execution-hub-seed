# Execution Hub Seed (可迁移开发包)

本文件夹用于在新设备上作为新仓库的起点，包含：
- 可直接复用的关键模块（已从现有仓库复制）
- 完整的文档系统（目标、需求、架构、数据模型、API、状态机、计划）

请从文档索引开始阅读：
- docs/INDEX.md

## 目录结构
- docs/                开发目标/需求/架构/数据模型/接口/计划
- modules/ids/         从原仓库复制的可复用模块（保持原路径结构）

## 重要说明
- modules/ids/ 仅作为“可复用参考实现”，不保证在新仓库中直接编译；需要调整模块路径与依赖。
- 文档系统中已明确“必须复用的兼容点”（状态机、证据链、审计字段、Trace 规范）。
