# 目标与非目标

目标
- 构建可运行的“任务执行中枢”，具备任务/步骤编排、执行调度、人机交接、证据链、审计回放能力。
- 提供可复用的 SDK/服务雏形，使任意 Agent 或应用可接入并获得执行可信度。
- 展示任务执行的可验证、可追责、可回放特性。

非目标
- 不做完整的通用项目管理工具（不替代 Jira/Notion）。
- 不追求复杂 UI 或全量权限体系（保留最小权限与审计）。
- 不实现复杂的企业级多租户与计费体系。

成功标准
- 有一个端到端 Demo 场景跑通。
- 能展示每个步骤的执行证据与审计轨迹。
- 能体现失败处理、超时与升级策略。
